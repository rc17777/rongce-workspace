# 审计底稿质量自动评分规则 v1.0

> 基于「程峰标准」+ 新人常见错误 + 三级复核制提炼
> 适用层级：L1 项目组内复核（逐页检查）
> 来源：微信公众号《审计底稿编制指南（上）》+ 融策审计智析Agent v2架构

---

## 一、评分维度与权重

总评分 = 加权求和（四维核心指标 + 三大扣分项），满分 100。

### 1.1 四维核心指标（每项 0-25 分，共 100 分）

| 维度 | 满分 | 核心问题 | 对应程峰标准 |
|------|------|----------|-------------|
| A. 目标明确性 | 25 | 是否写明对应哪项/哪些认定？ | 标准一 |
| B. 过程清晰性 | 25 | 样本选取方法和测试步骤是否说清？ | 标准二 |
| C. 证据充分性 | 25 | 结论是否有完整证据链支撑？ | 标准三 |
| D. 索引完整性 | 25 | 交叉引用是否可追踪？ | 标准四 |

### 1.2 三大扣分项（每项 -5 到 -15 分）

| 扣分项 | 最高扣分 | 触发条件 |
|--------|---------|----------|
| E. 有数无说 | -10 | 数字堆砌但缺乏文字分析说明 |
| F. 有论无据 | -15 | 结论明确但缺失支撑依据 |
| G. 照抄嫌疑 | -5 | 与上年底稿高度雷同且无本年度差异化分析 |

---

## 二、逐维度评分标准

### 维度 A：目标明确性（0-25 分）

| 分数 | 等级 | 判定标准 |
|------|------|----------|
| 25 | 优秀 | 明确写出本程序对应的所有相关认定（如存在性、完整性、准确性、截止等），且认定选择与程序性质完全匹配 |
| 20 | 良好 | 写明了主要认定，但遗漏了次要相关认定 |
| 15 | 合格 | 写明了认定，但表述笼统（如只写"验证真实性"而没有用标准认定术语） |
| 10 | 不足 | 目标描述模糊，如"检查应收账款"而未说明验证什么 |
| 5 | 严重不足 | 只有程序名称，无任何目标说明 |
| 0 | 缺失 | 无目标部分 |

**自动检测规则**：
```
IF 底稿中不包含以下任一关键词: ["存在", "完整性", "准确性", "截止", "计价", "权利", "列报", "认定", "验证", "目标"]
THEN 标记为"目标缺失"，score_A = 0

IF 包含关键词但未使用标准认定术语
THEN score_A ≤ 15

IF 使用了标准认定术语 AND 认定与程序类型不匹配
THEN score_A ≤ 15
```

---

### 维度 B：过程清晰性（0-25 分）

| 分数 | 等级 | 判定标准 |
|------|------|----------|
| 25 | 优秀 | 完整说明：抽样方法类型+选取逻辑+样本量+覆盖比例+具体测试步骤+核查的文件类型 |
| 20 | 良好 | 说明了方法和样本量，但缺少覆盖比例或文件类型 |
| 15 | 合格 | 说明了方法但未说明选取逻辑 |
| 10 | 不足 | 只写了样本量和"测试无异常"，未说明抽样方法 |
| 5 | 严重不足 | 仅写"已检查"或"已测试" |
| 0 | 缺失 | 无过程描述 |

**自动检测规则**：
```
抽样方法关键词库: ["随机抽样", "分层抽样", "大额优先", "PPS抽样", "系统抽样", "金额降序", "按金额", "选取", "抽取"]
测试步骤关键词库: ["获取", "核对", "检查", "验证", "比较", "重新计算", "函证", "盘点", "观察", "询问"]
文件类型关键词库: ["合同", "发票", "出库单", "签收单", "银行对账单", "凭证", "明细账", "权证", "评估报告"]

score_B_base = 0
IF 包含抽样方法关键词: score_B_base += 10
IF 包含测试步骤关键词: score_B_base += 5
IF 包含文件类型关键词: score_B_base += 5
IF 包含覆盖比例/金额占比: score_B_base += 5
IF 以上全无: score_B_base = 0
```

---

### 维度 C：证据充分性（0-25 分）

| 分数 | 等级 | 判定标准 |
|------|------|----------|
| 25 | 优秀 | 每个结论都有明确证据支撑；证据与认定一一对应；例外事项有记录和处理说明 |
| 20 | 良好 | 主要结论有证据，但个别次要结论证据不够具体 |
| 15 | 合格 | 有证据，但结论与证据之间的逻辑关系需复核人自行推断 |
| 10 | 不足 | 证据不完整，缺少关键证据类型 |
| 5 | 严重不足 | 结论基本无证据支撑 |
| 0 | 缺失 | 只有结论，完全无证据 |

**自动检测规则**：
```
证据关键词库: ["依据", "证据", "支撑", "经检查", "经核查", "经抽查", "来源", "详见索引", "参照", "根据"]

IF 底稿中每一条结论后面都紧跟证据描述: score_C = 25
IF 有结论但部分缺少对应证据: score_C = 10~20
IF 有结论但完全无证据支撑: score_C = 0

例外处理检测:
IF 底稿包含"差异"/"异常"/"不符"/"例外" 关键词
AND 未包含处理说明: score_C -= 5
```

---

### 维度 D：索引完整性（0-25 分）

| 分数 | 等级 | 判定标准 |
|------|------|----------|
| 25 | 优秀 | 所有引用均有索引号；索引格式统一；交叉引用可形成完整闭环 |
| 20 | 良好 | 主要引用有索引，少量引用缺少索引号 |
| 15 | 合格 | 有索引但格式不统一，部分索引无法定位 |
| 10 | 不足 | 仅有零星索引，大部分引用无索引 |
| 5 | 严重不足 | 几乎无索引 |
| 0 | 缺失 | 完全无索引系统 |

**自动检测规则**：
```
索引模式: 正则匹配底稿编号模式（由系统配置底稿编码规则）
交叉引用检测: 索引是否在底稿集合中可查找到对应文件

IF 索引覆盖率 >= 90% AND 交叉引用闭环率 >= 90%: score_D = 25
IF 索引覆盖率 >= 70%: score_D = 20
IF 索引覆盖率 >= 50%: score_D = 15
IF 索引覆盖率 < 50%: score_D = 10
IF 无索引: score_D = 0
```

---

## 三、扣分规则

### E. 有数无说（0 到 -10 分）

```
计算底稿中数字内容占比 vs 文字分析占比:
IF 数字/总内容 > 0.7 AND 文字分析 < 200字: 触发扣分
扣分 = min(10, (数字占比 - 0.5) * 20)
```

### F. 有论无据（0 到 -15 分）

```
遍历所有结论句（匹配"结论:"/"结论："等模式）:
FOR EACH 结论:
    IF 结论后 500 字符内无证据关键词: 该结论标记为"无据"
扣分 = 无据结论数 * 5，上限 -15
```

### G. 照抄嫌疑（0 到 -5 分）

```
与上年度同项目底稿做文本相似度对比:
IF 相似度 > 0.9 AND 无本年度差异化标注: 扣 -5
IF 相似度 > 0.7 AND 无本年度差异化标注: 扣 -2
```

---

## 四、总分计算与质量等级

```
原始总分 = score_A + score_B + score_C + score_D
扣分总额 = penalty_E + penalty_F + penalty_G
最终得分 = max(0, 原始总分 + 扣分总额)  // 扣分项为负值

质量等级:
≥ 90 分 : A 级 — 优秀底稿，可直接通过 L1 复核
80-89 分: B 级 — 良好底稿，少量修改后通过
70-79 分: C 级 — 合格底稿，需补充完善
60-69 分: D 级 — 不足底稿，退回修改
< 60 分 : F 级 — 不合格底稿，退回重做
```

---

## 五、与三级复核的对接

### 5.1 L1 复核（项目组内）→ 使用本评分规则
- 逐页检查，使用四维核心指标+三大扣分项
- **通过阈值：≥ 70 分**
- 低于 70 分 → 退回助理修改

### 5.2 L2 复核（部门经理）→ 关注结论层
- 不逐页评分，而是关注：
  - 关键审计事项的底稿得分是否 ≥ 80
  - 高风险领域的底稿是否完整
  - 审计意见是否有充分底稿支撑

### 5.3 L3 复核（独立质量复核 Agent）→ 全局质量评分
- 使用独立评分体系（见 v2 架构文档 4.3 节）
- **否决阈值：< 0.85（归一化后）**
- 对应本评分规则：最终得分 < 72 分触发否决风险

---

## 六、Agent 实现接口

```python
class WorkpaperScorer:
    """
    底稿质量自动评分引擎
    输入：单张底稿的 Markdown/JSON 结构化内容
    输出：评分报告
    """
    
    def score(self, workpaper: Workpaper, previous_year: Optional[Workpaper] = None) -> ScoreReport:
        # 四维核心评分
        score_a = self._score_target_clarity(workpaper)
        score_b = self._score_process_clarity(workpaper)
        score_c = self._score_evidence_sufficiency(workpaper)
        score_d = self._score_index_completeness(workpaper)
        
        # 三大扣分项
        penalty_e = self._penalty_numbers_without_analysis(workpaper)
        penalty_f = self._penalty_conclusion_without_evidence(workpaper)
        penalty_g = self._penalty_copy_last_year(workpaper, previous_year)
        
        # 总分
        raw_score = score_a + score_b + score_c + score_d
        penalty_total = penalty_e + penalty_f + penalty_g
        final_score = max(0, raw_score + penalty_total)
        
        # 质量等级
        grade = self._map_to_grade(final_score)
        
        return ScoreReport(
            final_score=final_score,
            grade=grade,
            dimension_scores={
                "A_目标明确性": score_a,
                "B_过程清晰性": score_b,
                "C_证据充分性": score_c,
                "D_索引完整性": score_d,
            },
            penalties={
                "E_有数无说": penalty_e,
                "F_有论无据": penalty_f,
                "G_照抄嫌疑": penalty_g,
            },
            passed_l1=final_score >= 70,
            risk_flags=self._generate_risk_flags(score_a, score_b, score_c, score_d),
            improvement_checklist=self._generate_checklist(final_score, grade)
        )
```

---

## 七、速查清单（Agent 自检用）

当 Agent 协助审计人员编制底稿时，自动检查以下 6 项：

| # | 检查项 | 规则 | 未通过动作 |
|---|--------|------|-----------|
| 1 | 目标 | 是否包含认定术语？ | 提示补充目标段 |
| 2 | 过程 | 是否包含抽样方法和测试步骤？ | 提示补充过程描述 |
| 3 | 结论 | 每条结论是否紧跟证据？ | 提示补充证据引用 |
| 4 | 索引 | 引用是否有索引号？ | 提示补充索引 |
| 5 | 依据 | 每个数字是否有来源标注？ | 提示标注数据来源 |
| 6 | 差异 | 异常事项是否记录和处理？ | 提示记录例外事项 |
