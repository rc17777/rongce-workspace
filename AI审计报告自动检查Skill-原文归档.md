# AI审计报告自动检查 Skill — 原文归档

**来源**：微信公众号「逆行的狗」(nigonigo)，2026年7月
**原文链接**：https://mp.weixin.qq.com/s/bzFVFqV4qTeqX3ljrjN0OA
**归档日期**：2026-07-04

---

## 背景

作者4月份发布了SW审计工具箱的「报告检查」功能，能自动跑勾稽、查附注、做格式文本检查。但有一个老毛病：**横加竖加容易误报**。原因是AI既要理解表格结构、又要做数字加减——附注里稍微复杂一点（"其中："子项、百分比列、两期并列），AI就经常猜错该加哪些、不该加哪些。最后输出一大堆警告，得人一条一条筛。

## 核心设计理念

> **AI 只干语义理解的活，所有数字计算交给代码。**

三层分工：

### 第一层：AI 理解报告结构
Claude 先读完整份报告，识别四张报表在哪、合并口径和母公司口径怎么区分、附注每个科目对应哪张表。输出两个 JSON 文件：
- **manifest.json**：声明四表的列布局（项目名在第几列、期末数在第几列）
- **note_map.json**：科目到附注表的精确映射

### 第二层：代码机械执行
按 manifest 抽数，统一交给 `calculator.py` 做加减核对。支持千分位、括号负数、万元单位，容差 0.01，每条结果带 expected / actual / diff，完全可复现。

### 第三层：AI 做终审复核
代码批量检查完会产生大量结果，其中不少是误报。AI 再带着原文上下文，把误报剔除，把真错留下。

这一步很关键。以前的工具误报率高，是因为没有终审。现在 AI 会回到原文，判断「这个差异是真的数字错了，还是表格结构特殊导致的」。实际跑下来，**大部分误报都能被 AI 终审筛掉**，留下的基本是真问题。

## 检查能力

四类检查：

1. **报表间勾稽**：资产 = 负债 + 权益、利润表推导链、现金流量表净额合计
2. **表注勾稽**：覆盖 41 个主要科目，比如应收账款 = 账面余额 − 坏账准备
3. **附注内勾稽与横加竖加**：变动表 reconcile、明细之和 = 合计行
4. **文本与格式**：错别字、病句、公司名是否统一、页码是否连续

最终输出 **Excel + Markdown** 两份复核报告，带页码定位和原文摘录，只读不改源报告。

## Skill vs 工具箱

相比工具箱，Skill 更灵活：
- 规则文件可以直接改
- 可以列清单让AI帮忙加规则
- 不同事务所的报告格式差异很大 → 给用户一个可自调教的框架

## 执行效果

1. 故意改错"货币资金"合计数
2. 在 opencode 里调用 skill 对审计报告进行检查
3. 生成检查报告 → 故意改错的数字直接被检出
4. 没有以前工具箱中的误报
5. 最后有一项检查项的表，完整显示进行了哪些维度的检查

## 效率权衡

用 skill 比之前的效率工具**准确度高了很多**，但**效率低了很多**。

## 一句话总结

> 审计复核这种活，**算数必须交给代码，判断交给AI，AI还要再审一遍自己的判断**——三层分工，比让AI一次性包打天下要稳得多。

---

## 关键技术要素提取

| 要素 | 描述 |
|------|------|
| 报告结构理解 | AI读取完整报告 → manifest.json（列布局）+ note_map.json（科目映射） |
| 数字计算引擎 | calculator.py — 千分位/括号负数/万元单位/容差0.01 |
| 终审复核 | AI回原文上下文，区分真错误 vs 表格结构特殊导致的误报 |
| 输出格式 | Excel + Markdown，带页码定位和原文摘录 |
| 检查维度 | 报表间勾稽 / 表注勾稽 / 附注内横加竖加 / 文本格式 |
| 可拓展性 | 规则文件可改，用户可自调教 |
| 核心洞察 | AI只做语义理解 + 代码只做数字计算 + AI再审一遍自己的判断 |
