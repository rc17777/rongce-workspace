# AI审计翻凭证资源包 — 原文归档

> 来源：微信公众号「AI效能Lab」《AI 审计翻凭证资源包 (核心指令版)》
> 作者：aiEfficeLab
> 归档日期：2026-07-03
> 系列归属：v8 — AI审计交互模式/翻凭证专题

---

## 一、核心配置：持久化会话 (Persistent Session)

**作用**：让AI记住大型账套的结构，避免每次翻单都要重新上传文件或解释字段含义。

**设置方法**：在与AI交互前，先输入初始化指令：
> "请记住当前文件的列名和数据格式，并在后续会话中保持此上下文，除非我主动要求重置。"

**防坑提醒**：
- **数据脱敏**：上传前务必隐藏客户名称、敏感账号
- **会话刷新**：如果AI开始"胡言乱语"，通常是Session长度超出限制，需重新开启对话并复用初始化指令

---

## 二、四大场景翻查指令 (Prompt Templates)

| 类别 | 翻查逻辑/指令模板 | 审计关注点 |
|------|------------------|-----------|
| **应付账款 (AP)** | "请从序时账中筛选出摘要包含'货款'但借方科目非'库存商品/原材料'的凭证。" | 查跨期、查核算错误 |
| **应收账款 (AR)** | "找出贷方发生额前10名的非银行存款对应科目，并列出其摘要和日期。" | 查异常回款、查关联方往来 |
| **费用报销 (EXP)** | "筛选出同一报销人、同一金额在3天内重复出现的记录，并输出凭证号。" | 查重复报销、查大额异常 |
| **固定资产 (FA)** | "找出摘要包含'采购/购买/设备'且金额超过5000元，但未计入'固定资产'科目的记录。" | 查资本化与费用化误判 |

### 四大场景的业务逻辑

#### AP（应付账款）
- **指令语义**：货款应该对应存货类科目（库存商品/原材料），如果对应了别的科目 → 可能是跨期费用、虚假采购、科目错用
- **审计实质**：会计分录的科目对应关系校验——这是审计中最基础的"勾稽关系"检查

#### AR（应收账款）
- **指令语义**：应收账款减少（贷方）不应该是银行存款（那意味着正常收款），而应该关注非银行存款的贷方——可能是债务重组、关联方转账、坏账核销
- **审计实质**：应收账款的异常减少路径——识别可能的利润操纵或关联方利益输送

#### EXP（费用报销）
- **指令语义**：同一人、同一金额、3天内重复 → 典型的重复报销模式
- **审计实质**：报销数据的时空聚集检测——这是最简单的异常模式，但也是最常见的舞弊手段

#### FA（固定资产）
- **指令语义**：5000元以上设备采购却没有进入固定资产科目 → 费用化处理规避资产折旧
- **审计实质**：资本化 vs 费用化的判断边界——直接影响当期利润和资产负债率

---

## 三、进阶技巧：指令组合拳

### 分步指令
不要一次让AI查所有。操作顺序：
1. 先"列出异常"
2. 再"针对某几笔解释原因"
3. 最后"生成抽样表"

### 验证指令
> "请针对你筛选出的第X号凭证，在原文件中找到对应行并完整展示，以核对你的准确性。"

**本质**：一条自校验闭环——AI查结果 → AI自我核对 → 人工确认

---

## 四、方法论提炼

### 4.1 翻凭证的AI交互范式

```
初始化（持久化会话）
  ↓
场景指令（AP/AR/EXP/FA 四大模板）
  ↓
分步执行（列出异常 → 解释原因 → 生成抽样）
  ↓
自校验（原文件回查验证）
  ↓
人工确认
```

### 4.2 每条指令的通用结构

```
[筛选条件] + [排除条件] + [输出要求]

筛选条件：描述"要什么"（如"摘要包含货款"）
排除条件：描述"不要什么"（如"借方科目非库存商品"）
输出要求：描述"怎么呈现"（如"列出摘要和日期"）
```

### 4.3 与Agent架构的关系

| 翻凭证范式 | Agent架构对应 |
|-----------|-------------|
| 持久化会话（记住账套结构） | LangGraph Checkpoint（v2） |
| 场景指令（AP/AR/EXP/FA模板） | 工具触发条件（agent_config.py） |
| 分步执行（列异常→解原因→抽样） | 4步流水线（pipeline.py） |
| 自校验（原文件回查） | L1评分引擎 + 人机核验（v3/v6） |
| 人工确认 | 三级复核（v2） |

---

## 五、本文独有贡献

与已归档的5篇文章（v3-v7）相比，本文的独特增量在于：

1. **交互层视角**：前5篇覆盖方法论/架构/工具/统计方法，本文首次聚焦"AI交互模式"——如何用自然语言指令驱动AI执行审计任务
2. **Prompt Engineering for Audit**：提供4条可复用的审计专用Prompt模板，填补了"审计人员如何与AI对话"的空白
3. **翻凭证专项**：应付/应收/费用/固定资产——审计最基础的4类凭证翻查，不是高大上的统计分析，是最日常的操作
4. **自校验闭环**：AI查结果→AI自我核对→人工确认，一个完整的质量保障链
5. **数据脱敏提醒**：首次在系列文章中明确提到"上传前隐藏客户名称"的安全实践

---

*归档完成。下一步：与融策审计智析Agent架构对照合并。*
