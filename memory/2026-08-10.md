# 2026-08-10 记录

## 20:32-21:00 C盘体检 + 清理（用户指令）

### 体检结论（C盘 118GB，清理前仅剩 9GB）
**大头**：
- `workspace\.git` 6.13GB（历史里塞了 480MB rag_index.json + 61MB wps，多版本叠加）
- `.cache\modelscope` 6.28GB（BAAI/bge-reranker-large，同一模型 safetensors+bin+onnx 三格式冗余）
- `.cache\huggingface` 2.85GB（bge-reranker-large 2.09GB + embedding 小模型）
- `AppData\Roaming\Tencent` 3.52GB（微信 1.89 + 企微 1.32）
- `AppData\Roaming\kingsoft` 2.31GB（WPS）
- `Desktop` 3.36GB / `OneDrive` 2.33GB / `Documents` 2.36GB（数据，不动）

### 清理执行
1. **安全档**（~1.54GB）：`%TEMP%` 512MB + `.cache\codex-runtimes` 1.03GB（⚠️ codex 进程 PID 18016 当时在跑，删缓存不影响，下次自动重下）
2. **确认档**（8.36GB）：确认 reranker 哪份在用后删除：
   - 删 `.cache\modelscope\hub\BAAI`（6.28GB）
   - 删 `.cache\huggingface\hub\models--BAAI--bge-reranker-large`（2.09GB）

### reranker 归属排查结论（关键）
- **没有任何生产代码引用 bge-reranker-large**：
  - 智析 v2（D:\openclaw-workspace\zhixi-v2\app.py，端口5002，watchdog.ps1 守护）：rag_bridge 用智谱 API（glm-4-plus），无本地模型
  - data-analysis-agent（D:\openclaw-workspace\projects\data-analysis-agent\，默认端口5001，未运行）：rag_engine.py 用的是 **bge-reranker-v2-m3**（注释明确"可选，未安装时跳过ReRank"），缓存里根本没有 v2-m3
  - 5000 端口 = Neo4j 服务（prunsrv），无关
  - modelscope 下载脚本（temp\dl_qwen.py）下载的是 Qwen2.5-7B-Instruct 到 D:\models，与 reranker 无关
- 缓存的 bge-reranker-large 是 7月20日某次实验下载后弃用的孤儿缓存
- 保留：huggingface 里的 embedding 模型（text2vec、MiniLM、bge-embedding 可能被 RAG 用）

### 结果
C 盘剩余：9GB → **18.72GB**（共释放 ~9.9GB）

### 待办
- [ ] `.git` 瘦身（6.13GB）：filter-repo 清历史大文件，需用户确认（要 force push）
- [ ] 微信/企微/WPS 缓存清理（用户自行在客户端操作）
