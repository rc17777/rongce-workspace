# 2026-08-05 记忆

## 10:40 审盾数据理解底座：六入口适配器全部落地（v0.1→v0.5 提前完成）

### 任务
完成数据理解底座离线异构适配器剩下4个入口：PDF电子件 / PDF扫描件 / SQL dump / API

### 交付（scripts/data_profiling/adapters/）
| 文件 | 功能 | 测试结果 |
|:--|:--|:--|
| `common.py` | SDF公共层（清洗/类型推断/语义标注/质量分层） | ✅ |
| `csv_excel_adapter.py` | CSV/Excel→SDF桥接（编码检测/多Sheet/多行表头） | ✅ 2行4列 |
| `pdf_electronic.py` | pdfplumber电子件（文本层探测→表格/文本双模式） | ✅ 4行5列 角色全对 |
| `pdf_ocr_worker.py` | PaddleOCR worker（paddleocr环境运行，每页JSON+断点续跑） | ✅ conf=0.99 |
| `pdf_scanned.py` | 扫描件适配器（PDF拆分→子进程OCR→质量分层） | ✅ 4行 高质量 |
| `sql_dump.py` | SQL解析（CREATE TABLE+INSERT多值，编码检测） | ✅ 5行7列 NULL正确 |
| `api_adapter.py` | 配置驱动API（page/offset分页、字段映射、空页停止） | ✅ 54条3页 |
| `router.py` + `ingest.py` | 统一入口（自动识别类型→分派→SDF） | ✅ 全类型通过 |

### 关键修复
- SQL INSERT_RE 尾部 `;` 被 split_statements 剥离导致不匹配 → 改为 `(.*)$`
- CREATE_RE 终止标记把列级 `DEFAULT NULL` 误判为建表结束 → 只认表级选项（ENGINE=/DEFAULT CHARSET/COLLATE=/CHARSET=/;）

### 技术要点
- base环境已有 pdfplumber 0.11.9 / pymupdf / requests，无需装包
- paddleocr 2.7.3 环境无 PPStructure → 表格重建用 y聚类+x排序（仅多列行输出表格行）
- PDF扫描件支持大文件拆分（--chunk-pages 25默认）+ 断点续跑（--keep-chunks）
- OCR质量分层：≥95%自动入库 / 85-95%抽检 / <85%人工确认

### 产出文件
- README: `scripts/data_profiling/adapters/README.md`
- 测试夹具: `scripts/data_profiling/adapters/testdata/`（含本地API测试服务）
- 设计文档落地状态更新: `knowledge/strategy/审盾-数据理解底座-离线异构适配器设计-20260804.md`

### 待办
- v0.6：语义标注规则引擎完善 + 口径校验层接入SDF
- v1.0：增量更新（追加/替换/差异报告）
