# 2026-08-08 记录

## 09:12 若尔盖医保 OCR 结果归档（用户指令）

**背景**：8月1日 temp_ocr 目录凭空消失事故（21本结果丢失）后，用户要求将 OCR 结果归档备份。

### 执行内容
1. **本地归档**：`temp_ocr\output_new`（29个文档目录，3477文件，2.33GB）→ `D:\杂志资料\若尔盖医保OCR\`（robocopy /MT:16，0失败）
2. **补充数据**：`temp_ocr\fin_extracted`（6个账户历史明细 xlsx）→ 同上目录
3. **归档说明**：`README.md`（含文档清单/大小表/恢复方式）
4. **OneDrive 备份**：`D:\杂志资料\若尔盖医保OCR` → `OneDrive\融策备份\若尔盖医保OCR\`（3484文件，2.28GB，0失败）
5. **发现 OneDrive 进程未运行** → 手动启动（PID 11060），云同步后台进行中

### 校验结果
- 源 output_new 3477 文件 = D: 3484 − 7（6 xlsx + README）= OneDrive 3484 ✓
- robocopy /L 校验 D: 与源 3476/3477 全匹配，0 差异（README 为预期额外项）
- ⚠️ 经验：robocopy /L + /MT 的 Total 数字会少报（多线程列目录 bug），别信它，用 PowerShell 数文件数

### 待办闭环（8月1日遗留）
- [x] output_new 是否进 git → **确认未进 git**（git ls-files temp_ocr 只有脚本）。2.3GB 不该进 git，正确方案就是本次归档 + OneDrive，维持现状
- [x] OCR 结果备份 → 本次完成（本地 D: + OneDrive 双保险）
- [ ] 确认 OneDrive 云上传完成（下次心跳可抽查文件属性）

### 教训
- 大批量数据备份用 robocopy /MT 又快又稳（2.3GB / 78秒）
- 备份前先查目标进程（OneDrive）是否在跑，否则只是本地拷贝，白忙
