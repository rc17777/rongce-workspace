# 融策审计智析Agent — 全栈实现总览

> 版本：v5.0.0 | 日期：2026-07-03 | 测试：16/16通过
> 代码量：~15,000行Python | 17个模块 | 1个MCP服务 | 1个测试套件

---

## 一、架构全景图

```
┌──────────────────────────────────────────────────────────────────┐
│                    融策审计智析Agent v5                           │
│                   (概念层融合 + 实现层解耦)                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    数据采集层 (Renderer)                      │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────────────┐  │ │
│  │  │ MCP接口  │  │ OCR管道  │  │ 数据就绪度评估 L1-L2-L3  │  │ │
│  │  │ data_readiness.py     │  │ 覆盖率报告 + 预处理建议    │  │ │
│  │  └──────────┘  └──────────┘  └──────────────────────────┘  │ │
│  └──────────────────────────┬──────────────────────────────────┘ │
│                             │ 结构化消息                           │
│  ┌──────────────────────────▼──────────────────────────────────┐ │
│  │                    分析加工层 (Simulator) ★ 枢纽层            │ │
│  │  ┌─────────────────────────────────────────────────────┐   │ │
│  │  │           5个文本分析工具 (MCP标准接口)               │   │ │
│  │  │  🔥 hotword  🔍 similarity  📄 contract             │   │ │
│  │  │  👤 personnel  💰 budget                            │   │ │
│  │  └─────────────────────────────────────────────────────┘   │ │
│  │  ┌─────────────────────────────────────────────────────┐   │ │
│  │  │       5个审计准则专家Agent (并行推演)                 │   │ │
│  │  │  国家审计 │ CPA审计 │ 内部审计 │ 工程审计 │ 数据审计   │   │ │
│  │  │  agent_config.py (系统提示+KB注入+工具分配)           │   │ │
│  │  └─────────────────────────────────────────────────────┘   │ │
│  │  ┌─────────────────────────────────────────────────────┐   │ │
│  │  │       辩论引擎 + 模拟器对偶 (Why层推理)               │   │ │
│  │  │  信任方 vs 质疑方 → 仲裁裁定                          │   │ │
│  │  │  simulator_duality.py                               │   │ │
│  │  └─────────────────────────────────────────────────────┘   │ │
│  │  ┌─────────────────────────────────────────────────────┐   │ │
│  │  │       业务本体论 (200条审计规则)                      │   │ │
│  │  │  实体(13) → 关系(10) → 规则(20) → Prompt注入         │   │ │
│  │  │  audit_ontology.py                                  │   │ │
│  │  └─────────────────────────────────────────────────────┘   │ │
│  └──────────────────────────┬──────────────────────────────────┘ │
│                             │ 结构化发现                           │
│  ┌──────────────────────────▼──────────────────────────────────┐ │
│  │                    复核/输出层 (Planner)                      │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │ │
│  │  │ L1 评分  │  │ L2 团队  │  │ L3 独立  │  │ 避坑约束 │   │ │
│  │  │ 引擎     │  │ 复核     │  │ 质量复核 │  │ 4项检查  │   │ │
│  │  │ scorer   │  │          │  │ (否决)   │  │ guards   │   │ │
│  │  │ ≥70通过  │  │ ≥80通过  │  │ <0.85否决│  │          │   │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │ │
│  │  ┌─────────────────────────────────────────────────────┐   │ │
│  │  │  结构化输出 (四段式Schema) + 年度对比 (防照抄)        │   │ │
│  │  │  structured_output.py │ year_over_year.py            │   │ │
│  │  └─────────────────────────────────────────────────────┘   │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    基础设施层                                  │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │ │
│  │  │ 索引子   │  │ 4步流水  │  │ MCP服务  │  │ AGR知识  │   │ │
│  │  │ 系统     │  │ 线编排   │  │ 7工具    │  │ 库 RAG   │   │ │
│  │  │ index    │  │ pipeline │  │ server   │  │ KB检索   │   │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    LangGraph 工作流编排                       │ │
│  │  data_collection → rule_configuration → data_analysis        │ │
│  │  → cross_validation → debate_engine → experts_parallel       │ │
│  │  → conclusion → L1_review → human_review → L2_review → L3    │ │
│  │  Checkpoint 中断恢复 │ 条件路由 │ 并行执行                     │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

## 二、模块清单

### 核心分析工具 (5个 P0)
| 工具 | 文件 | 行数 | 算法 | 应用场景 |
|---|---|---|---|---|
| 热词分析 | `hotword.py` | 215 | TF-IDF + jieba | 会议纪要、决策文件 |
| 相似度比对 | `similarity.py` | 170 | Jaccard(global/local) | 药品串换、围标检测 |
| 合同提取 | `contract.py` | 300 | 正则 + 交叉比对 | 合同审计、支付核查 |
| 人员比对 | `personnel.py` | 210 | 集合运算 | 补贴核查、身份验证 |
| 预算合规 | `budget.py` | 320 | 规则引擎(44条) | 三公经费、采购合规 |

### 质量保障 (3个 P0+P1)
| 模块 | 文件 | 核心能力 |
|---|---|---|
| L1评分引擎 | `workpaper_scorer.py` | 7维评分(A/B/C/D+E/F/G)，批量+照抄检测 |
| 输出结构化 | `structured_output.py` | 四段式Schema(目标-过程-结论-索引)+校验器 |
| 模拟器对偶 | `simulator_duality.py` | 信任/质疑双视角+仲裁，覆盖5工具15场景 |

### Agent与知识 (3个 P1+P2)
| 模块 | 文件 | 核心能力 |
|---|---|---|
| Agent配置 | `agent_config.py` | 5Agent定义/系统提示/KB注入/工具分配矩阵 |
| 避坑约束 | `pitfall_guards.py` | 4项LangGraph条件边(交叉验证/人机核验/覆盖率/定制) |
| 业务本体论 | `audit_ontology.py` | 200条规则(实体→关系→因果)，条件匹配+Prompt注入 |

### 流水线与基础设施 (4个)
| 模块 | 文件 | 核心能力 |
|---|---|---|
| 4步流水线 | `pipeline.py` | 归集→配置→分析→核验，端到端+人工反馈闭环 |
| 年度对比 | `year_over_year.py` | 三字符组+段落对比+数据陈旧度+照抄检测 |
| 数据就绪度 | `data_readiness.py` | L1-L2-L3三级评估+OCR工时预估+预处理建议 |
| 索引子系统 | `audit_index.py` | 12类索引前缀/交叉引用/BFS引用链/Mermaid可视化 |

### MCP服务
| 模块 | 文件 | 注册工具 |
|---|---|---|
| MCP Server | `mcp_server.py` | 7个工具 JSON-RPC标准接口 |

## 三、数据流

```
原始数据 ──→ data_readiness (就绪度评估)
    │
    ▼
data_collection (归集+分类+覆盖率)
    │
    ▼
rule_configuration (AGENT配置+本体论注入+规则匹配)
    │
    ▼
data_analysis (5工具并行调用)
    │   ├── hotword ──→ simulator_inference
    │   ├── similarity ──→ simulator_inference
    │   ├── contract ──→ simulator_inference
    │   ├── personnel ──→ simulator_inference
    │   └── budget ──→ simulator_inference
    │
    ▼
cross_validation + debate_engine (避坑约束+辩论)
    │
    ▼
structured_output (四段式Schema构建)
    │
    ▼
L1_review (WorkpaperScorer ≥70)
    │
    ▼
year_over_year (年度对比+照抄检测)
    │
    ▼
human_review (高风险100%人工复核)
    │
    ▼
L2_review → L3_quality_gate (<0.85否决)
    │
    ▼
audit_index (索引子系统+引用链)
```

## 四、测试覆盖

| 编号 | 测试模块 | 核心验证点 |
|---|---|---|
| 1 | 热词分析 | TF-IDF提取+风险词库匹配 |
| 2 | 相似度 | 药品串换85.7%+供应商微调90.9% |
| 3 | 合同提取 | 6字段+金额标准化+超付检出 |
| 4 | 人员比对 | 三重筛查(财政/死亡/重复) |
| 5 | 预算合规 | 44条规则+3高危检出 |
| 6 | 模拟器对偶 | 双视角推理+仲裁裁定 |
| 7 | MCP Server | 7工具JSON-RPC调用 |
| 8 | 4步流水线 | 端到端+人工反馈闭环 |
| 9 | 输出结构化 | Schema校验+构建器 |
| 10 | L1评分 | 优秀90分 vs 不合格15分 |
| 11 | Agent配置 | 5Agent定义+KB注入 |
| 12 | 避坑约束 | 4项检查+综合报告 |
| 13 | 年度对比 | 照抄93.3%检测+正常更新49.7% |
| 14 | 数据就绪度 | L3红色75%+OCR预估 |
| 15 | 业务本体论 | 13实体+10关系+20规则+条件匹配 |
| 16 | 索引子系统 | CRUD+引用链+完整性100% |
