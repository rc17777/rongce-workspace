# AI审计报告自动检查Skill × 融策审计智析Agent — 对照合并

**基于**：「逆行的狗」《审计复核：AI理解、代码算数、AI再审一遍》（2026-07）
**对照版本**：融策审计智析Agent v11
**分析日期**：2026-07-04

---

## 一、概念映射矩阵（14项）

| # | 原文概念 | Agent架构对应 | 匹配度 | 说明 |
|---|----------|-------------|--------|------|
| 1 | 三层分工（AI理解→代码执行→AI终审） | 三级复核（L1自动复核→L2审计准则专家→L3独立质量复核） | 🟡 部分 | 都是三层，但方向不同：原文三层是**计算流水线**，Agent三层是**质量门禁**。互补而非替代 |
| 2 | manifest.json — 报告结构声明 | 无直接对应 | 🔴 未覆盖 | Agent没有报告结构解析层，只处理离散数据 |
| 3 | note_map.json — 科目到附注映射 | 无直接对应 | 🔴 未覆盖 | Agent缺少"审计报告文档对象模型" |
| 4 | calculator.py — 纯代码算数 | text_hotword_analysis 等工具集 | 🟡 部分 | Agent工具集有计算逻辑，但**未做AI语义理解与数字计算的刚性分离** |
| 5 | 横加竖加（明细之和=合计） | 无对应 | 🔴 未覆盖 | Agent的Benford/异常检测面向行为模式，不面向表格算术校验 |
| 6 | 报表间勾稽（资产=负债+权益等） | 无对应 | 🔴 未覆盖 | Agent关注异常信号，不关注基础勾稽关系 |
| 7 | 表注勾稽（41科目） | 无对应 | 🔴 未覆盖 | Agent缺少会计科目层面的对账逻辑 |
| 8 | 终审复核（AI筛误报） | L3独立质量复核 | 🟢 完全 | 概念100%一致——都有独立的AI终审环节剔除误报 |
| 9 | 可调教规则框架 | Agent配置（agent_config.py） | 🟢 完全 | 都是可配置、用户可自定义的规则体系 |
| 10 | 输出带页码定位+原文摘录 | evidence_chain_graph.py（证据链图谱） | 🟡 部分 | Agent有证据链输出，但缺页码定位和原文摘录 |
| 11 | Excel+Markdown双格式输出 | 各工具独立输出 | 🟡 部分 | Agent缺少统一的"复核报告"输出层 |
| 12 | 误解表结构→误报 | v5非结构化预处理评估 | 🟡 部分 | v5评估纸张就绪度，不涉及表结构理解错误 |
| 13 | 效率降低换取准确度提升 | 三级复核设计初衷 | 🟢 完全 | 都是"牺牲效率换取质量"的trade-off |
| 14 | Skill可分发框架 vs 固定工具箱 | Agent插件化架构 | 🟢 完全 | 都是模块化、可拓展的架构 |

**匹配度统计**：完全覆盖 4/14（29%）| 部分覆盖 5/14（36%）| 未覆盖 5/14（36%）

**匹配度低于v3-v11平均水平（~60%完全+部分），说明本文引入了一类Agent架构中尚未系统涉及的能力——审计报告文档检查。**

---

## 二、关键差异分析

### 差异1：计算流水线 vs 异常检测
- **原文**：面向**已知规则**的批量执行——横加竖加、报表间勾稽、表注勾稽，全部是确定的算术规则
- **Agent**：面向**未知模式**的智能发现——Benford异常、供应商指纹、时间序列偏离，全部是统计推断
- **关系**：互补而非替代。Agent适合"发现隐藏问题"，Skill适合"验证已知规则"

### 差异2：报告文档对象模型
- **原文**：有完整的manifest.json（列布局）+ note_map.json（科目映射），构建了报告的**结构化数字孪生**
- **Agent**：直接处理原始数据流，无报告级文档模型
- **差距**：这是Agent最显著的空白——无法自动理解一份Word/PDF审计报告的表格结构

### 差异3：误报率的系统解法
- **原文**：终审复核AI回到原文上下文，区分"真错误"vs"表格结构特殊"——**误报问题有独立解决流程**
- **Agent**：辩论引擎+评审，是对分析层结果的质量控制，不针对"表格结构理解错误"这类特定误报源

---

## 三、架构增强方案（4项）

### 增强1：审计报告文档解析器 ReportParser（P0，新增，7天）

**对应原文**：第一层 — AI理解报告结构（manifest.json + note_map.json）

**方案**：
- 新增 `report_parser.py`（~500行）
- 输入：审计报告Word/PDF
- 输出：
  - `report_structure.json`：四表位置、合并vs母公司口径、列布局、数字格式（千分位/括号/万元）
  - `account_note_map.json`：科目→附注表映射（≥41科目）
  - `cross_ref_rules.json`：可检查的勾稽关系清单
- 依赖：pdf + officecli-docx skill（已有）
- 分配给：数据层Agent（数据采集层）

**价值**：补齐Agent最大的结构性空白——从"处理离散数据"升级为"理解完整审计报告"

### 增强2：四场景通用算术勾稽引擎 ArithmeticCrossChecker（P0，新增，10天）

**对应原文**：第二层 — 代码机械执行（calculator.py）

**核心设计原则**：原文勾稽引擎硬编码"四表一注"，仅适用于年报审计。融策实际业务涵盖政府审计、工程咨询、绩效评价等多种形态，勾稽引擎必须是一个**规则驱动、场景可配、表结构无关**的通用执行框架。

#### 2.1 四层引擎总架构

```
┌──────────────────────────────────────────────────────────┐
│                    第一层：TableModel                     │
│   通用表格抽象 — 任何Word/PDF表格 → [{sheet, columns,     │
│   rows}], 不预设"四表一注"语义                            │
├──────────────────────────────────────────────────────────┤
│                  第二层：DomainAdapter                   │
│   ┌────────────┬────────────┬────────────┬────────────┐  │
│   │  年报适配器  │ 专项审计    │ 工程咨询    │ 绩效评价    │  │
│   │  BS/IS/CF/  │ 预算vs实际  │ 五算对比    │ 权重闭合    │  │
│   │  权益变动表  │ 任期对比    │ 清单vs定额  │ 加权递推    │  │
│   │  41科目映射 │ 整改追踪    │ 变更增减    │ 自评复评分立│  │
│   │  合并vs母   │ 问题汇总    │ 进度款核验  │ 三率互证    │  │
│   └────────────┴────────────┴────────────┴────────────┘  │
├──────────────────────────────────────────────────────────┤
│                  第三层：RuleEngine                      │
│   规则语言：表达式 = CellRef | Agg(exprs) | EXPR op EXPR │
│             | IF(cond, then, else)                       │
│   三类规则模板 + 终审过滤                                 │
├──────────────────────────────────────────────────────────┤
│                 第四层：ReviewFilter                     │
│   AI回原文上下文 → confirmed_error / false_positive /     │
│   needs_human_review                                    │
└──────────────────────────────────────────────────────────┘
```

#### 2.2 四场景规则包

| 场景 | 规则包 | 规则数 | 核心算术逻辑 | 典型规则示例 |
|------|--------|--------|-------------|-------------|
| **年报审计** | annual_report | ~60 | 报表间等式+附注横加竖加+41科目表注+文本格式 | A=L+E, 利润表推导, 应收账款=账面余额−坏账准备, 明细之和=合计 |
| **专项审计** | special_audit | ~40 | 预算vs实际/任期对比/专项资金收支平衡/问题金额汇总/整改追踪 | 预算≥实际, 期初+本期增加−本期减少=期末, 问题金额≥0 |
| **工程咨询** | engineering | ~50 | 五算对比(概/预/标/结/决)/清单量vs定额/变更增减/材料调差/进度款 | 概算≥预算≥合同价≥结算价≥决算价, 进度款=完成量×单价−已付, 变更量=变更后−变更前 |
| **绩效评价** | performance | ~45 | 权重闭合/加权递推/自评复评分立/三率互证/多源取低/目标完成度 | Σ同级权重=100%, 上级得分=Σ(下级得分×权重), 到位率vs使用率vs执行率互证, 现场量<自报量→取现场量 |

#### 2.3 绩效评价规则包详解（关键差异化场景）

绩效评价的算术逻辑与年报/专项/工程有本质差异——不勾稽资产负债，勾稽的是**指标体系的数学一致性**和**评分过程的程序正确性**。

| 勾稽类型 | 规则 | 算式 | 说明 |
|----------|------|------|------|
| 权重闭合 | Σ(同级指标权重) = 100% | `SUM(weights) == 1.0` @ 容差0.0001 | 三级指标权重加起来≠100% → 指标体系本身有错 |
| 加权递推 | 上级得分 = Σ(下级得分 × 下级权重) | `score[i] == SUM(child[j].score * child[j].weight)` | 递归验证每一层。人工打分表最常出现"算错加权" |
| 自评复评分立 | 自评用自评原始分计算综合分 vs 复评用复评分计算综合分 | 两条独立递推链各自计算 → 对比差异 | 常见舞弊：自评虚高但复合权重掩盖了。差异>5分→标记"评分尺度不一致" |
| 资金效率三率 | 到位率/使用率/预算执行率 互相印证 | `到位率==1.0 ∧ 使用率<0.5 → "资金沉淀"`；`到位率<1.0 ∧ 执行率>1.0 → "逻辑矛盾"` | 三个率各自算对不保证整体正确——必须交叉验证 |
| 目标完成度 | 实际值/目标值 → 阈值判定 | `IF(实际/目标≥0.9, full_score, IF(实际/目标≥0.8, full_score×0.8, ...))` | 规则可配：90%满分/80%得80%/差1%扣1分/差1%扣0.5分 |
| 多源核验 | 台账数 vs 财务账 vs 现场抽查数 | `MIN(台账, 财务账, 现场抽查)` 取最低值重新计分 | 三数不一致时按审计保守原则取最低值 |

#### 2.4 规则引擎规则语言（Rule DSL）

```
表达式类型：
  1. 单元格引用：     Sheet!Row[条件].Column[条件]
  2. 聚合：          SUM(exprs) | COUNT(exprs) | AVG(exprs) | MAX(exprs) | MIN(exprs)
  3. 四则运算：       expr + expr | expr - expr | expr * expr | expr / expr
  4. 条件分支：       IF(cond, then, else)
  5. 比较：          == | != | >= | <= | > | <

规则元数据：
  {id, domain, type, expression, tolerance, severity, requires_human_review, 
   external_thresholds, description}
```

#### 2.5 外部阈值配置文件

绩效评价和工程咨询的大量子规则依赖外部阈值，不能硬编码：

```yaml
# thresholds/performance_default.yaml
budget_execution:
  low_warning: 0.8      # 预算执行率低于80%→预警
  critical: 0.6          # 低于60%→重大问题
fund_efficiency:
  fund_arrival_ok: 1.0   # 到位率100%=正常
  usage_low: 0.5          # 使用率低于50%→资金沉淀
  logic_contradiction:    # 到位率<执行率→逻辑矛盾
    enabled: true
deduction_rules:
  mode: "per_point"      # per_point: 差1%扣1分 | per_half_point: 差1%扣0.5分
  per_point_value: 1.0
multi_source:
  rule: "min"            # min: 取最低值 | avg: 取平均值 | manual: 人工判断
```

**价值**：将"年报四表检查器"升级为**四场景通用算术勾稽引擎**，使年报/专项/工程/绩效四种审计形态共享同一执行框架、各自定义规则语义和阈值，覆盖融策全部核心业务线。

### 增强3：终审误报过滤层 ReviewFilter（P0，新增，3天）

**对应原文**：第三层 — AI做终审复核

**方案**：
- 新增 `review_filter.py`（~300行）
- 功能：
  - 接收 ArithmeticCrossChecker 的大量输出
  - 对每条结果，回原文上下文做二次判断
  - 分类：`confirmed_error` / `false_positive` / `needs_human_review`
  - 误报理由（结构化）：表结构特殊 / "其中："子项 / 百分比列 / 两期并列 / 单位不一致
- 过滤后输出：`filtered_issues.json`（只保留confirmed + needs_human_review）
- 集成到L1自动复核流程（评分引擎之前）

**价值**：解决"大量误报→人逐条筛"的痛点，与原文三层分工理念完全对齐

### 增强4：统一复核报告生成器 ReviewReportGenerator（P1，新增，3天）

**对应原文**：Excel + Markdown双格式输出，带页码定位和原文摘录

**方案**：
- 新增 `review_report_generator.py`（~350行）
- 输入：filtered_issues.json + 原报告文件
- 输出：
  - `复核报告.xlsx`：按检查维度分sheet（报表间/表注/附注内/文本格式），含page_ref + excerpt + expected/actual/diff
  - `复核报告.md`：可读Markdown，含交叉引用链接
- 集成到报告输出层Agent

**价值**：统一的"审计复核报告"输出能力——此前各工具独立输出，缺少汇总报告

---

## 四、修订后全局优先级

### P0（立即实施，20天）
| # | 增强 | 工时 | 说明 |
|---|------|------|------|
| 1 | ReportParser 报告文档解析器 | 7天 | 补齐Agent最大结构性空白 |
| 2 | ArithmeticCrossChecker 四场景算术勾稽引擎 | 10天 | 年报+专项+工程+绩效，四层引擎+规则DSL+阈值配置 |
| 3 | ReviewFilter 终审误报过滤层 | 3天 | 解决误报率痛点 |
| **P0小计** | | **20天** | |

### P1（短期，3天）
| # | 增强 | 工时 | 说明 |
|---|------|------|------|
| 4 | ReviewReportGenerator 统一复核报告 | 3天 | 整合输出层 |
| **P1小计** | | **3天** | |

### 此前待实施（来自v6/v8/v10/v11）
| 来源 | 增强 | 工时 | 状态 |
|------|------|------|------|
| v6 | 用户反馈持续学习 | 8天 | 待实施 |
| v10 | 审计时效监控 | 3天 | 待实施 |
| v10 | 司法协同接口 | 5天 | 待实施 |
| v11 | 决策程序审计追踪 | 5天 | 待实施 |
| **此前待实施小计** | | **21天** | |

### 全栈总计
| 类别 | 工时 |
|------|------|
| 已完成（v3-v11） | 100天 |
| v12 新增P0+P1 | 23天 |
| 此前待实施 | 21天 |
| **剩余待实施** | **44天** |
| **全栈完成度** | **69%**（100/144） |

---

## 五、关键发现

### 1. 本文补上了Agent最大的功能空白
v3-v11的所有工具面向**数据分析与异常检测**（Benford、指纹、时间序列、围标串标等），而本文面向**文档复核与算术校验**。这是审计工作的两个基本面——前者是"找隐藏问题"，后者是"验已知规则"。Agent此前只有前者。

### 2. 三层分工理念是对Agent架构的精确补充
- Agent的**三级复核**（L1/L2/L3）= **质量门禁**，在分析结论上做质量控制
- 本文的**三层分工**（AI理解→代码执行→AI终审）= **计算流水线**，在执行过程中做分工解耦
- 两者正交：质量门禁保证"结论可信"，计算流水线保证"执行准确"

### 3. "AI只干语义，代码只干算数"是架构黄金法则
Agent内工具集（benford.py、supplier_fingerprint.py等）已经实现了这一原则——Python代码处理数值计算，AI负责解释和判断。但本文把它做了**显式声明和刚性分离**，使设计哲学从"隐含做法"升级为"明确架构约束"。

### 4. 准确度vs效率的权衡与Agent现有路径一致
"准确度高了很多，但效率低了很多"——这与Agent设计的"牺牲效率换质量"哲学完全一致。Agent引入辩论引擎、三级复核、独立评审，每一步都是效率换质量的代价。

### 5. 可调教框架理念与Agent插件化架构一致
"不同事务所格式差异很大 → 给一个可自己调教的框架"——与Agent模块化、可配置、可拓展的架构完全一致。

---

## 六、与已有工具的不重叠验证

| 已有工具 | 检查类型 | 与本文重叠？ |
|----------|----------|-------------|
| benford.py | 首位数字分布统计检验 | 无重叠——行为模式 vs 算术规则 |
| supplier_fingerprint.py | 供应商多维特征相似度 | 无重叠——供应商分析 vs 报表检查 |
| timeline_anomaly.py | 时间序列支付偏离 | 无重叠——时间维度 vs 报表维度 |
| contract_change_trajectory.py | 合同变更轨迹 | 无重叠——合同分析 vs 报表检查 |
| bid_rigging_detector.py | 围标5维特征 | 无重叠——投标行为 vs 报表检查 |
| duplicate_claim_detector.py | 三维联合去重 | 无重叠——费用舞弊 vs 报表检查 |
| three_flow_checker.py | 三流合一验证 | 无重叠——关联交易 vs 报表检查 |
| text_hotword_analysis.py | TF-IDF热词提取 | 无重叠——语义分析 vs 格式检查 |

**结论**：现有8个工具全部面向**数据分析与异常检测**，本文4项增强全部面向**文档结构与算术校验**——零重叠，纯增量。
