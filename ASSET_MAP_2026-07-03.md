# 融策 AI 资产地图（2026-07-03）

> 盘点范围：`C:\Users\scrccpa\.openclaw\workspace` 工作区、`knowledge` 知识库、`scripts` 脚本、`output/outputs` 输出成果、`RONGCE_AI_HUB` 统一中枢。  
> 22:00 先完成盘点；22:06-22:09 按建议执行了第一轮根目录归档，只移动明确的临时/归档候选文件，未删除文件。迁移记录见 `asset_migration_log_all_2026-07-03.csv`。

## 1. 总体判断

当前工作区已经形成了一个“审计 AI 作业中枢”的雏形，核心资产包括：

- 审计知识库：杂志/政策/案例/OCR 资料，约 1.1GB。
- 脚本工具库：OCR、RAG、案例采集、报告复核、串标分析、Excel/PPT/Word 生成等，约 704 个脚本/缓存文件。
- 输出成果库：历史项目成果、OCR 图片、投标分析、合同分析、案例 OCR，约 2.18GB。
- 方法中枢：`RONGCE_AI_HUB` 已经具备“技能、记忆、知识库、规则库、模型”的统一入口。

主要问题不是“没有资产”，而是资产入口太多、根目录临时文件偏多、脚本缺少分层、输出成果和中间文件混在一起。下一步重点应是“保成果、分脚本、收临时、建索引”。

## 2. 目录体量概览

| 目录 | 文件数 | 体量 | 当前状态 | 处理建议 |
|---|---:|---:|---|---|
| `output/` | 2066 | 2025.72 MB | 历史项目成果和大体量中间文件混合 | 重点治理，分“项目成果/中间文件/历史归档” |
| `knowledge/` | 1672 | 1088.41 MB | 核心知识库，价值高 | 保留，补索引和去重，不轻易动 |
| `.rag_index/` | 1 | 479.60 MB | RAG 索引文件 | 可重建，保留近期版本，旧版可归档 |
| `outputs/` | 391 | 150.85 MB | 近期 OCR/项目输出 | 保留成果，清理图片级中间件需确认 |
| `scripts/` | 704 | 52.23 MB | 工具资产丰富，但混杂 | 按功能分层，建立 README |
| `tmp/` | 15 | 3.24 MB | 临时处理文件 | 可归档/清理候选 |
| `memory/` | 85 | 0.35 MB | 历史上下文 | 保留，定期提炼到 `MEMORY.md` |
| `RONGCE_AI_HUB/` | 13 | 0.13 MB | 统一中枢 | 作为主入口继续建设 |

## 3. 可直接使用的资产

| 资产 | 路径 | 用途 | 建议动作 |
|---|---|---|---|
| 融策 AI 统一中枢 | `RONGCE_AI_HUB/` | 技能、记忆、知识库、规则库统一入口 | 设为主导航入口 |
| 审计规则库框架 | `RONGCE_AI_HUB/rule-library/` | 从案例提炼审计规则，支撑文本分析工具 | 继续补首批 50 条规则 |
| 专项审计报告复核模型 | `RONGCE_AI_HUB/models/` | 报告复核、底稿闭环、质控 | 作为报告复核默认标准 |
| RAG 查询脚本 | `scripts/rag_query.py`、`scripts/rag_rebuild.py`、`scripts/rag_server.py` | 本地知识库问答与索引重建 | 保留，补运行说明 |
| 案例采集系统 | `scripts/case_collector.py`、`case_classifier.py`、`case_archiver.py` | 审计案例采集、分类、归档 | 可投入周期性使用 |
| 案例采集说明 | `scripts/README_case_collection.md` | 使用流程文档 | 保留，作为操作说明 |
| Token/成本守卫 | `scripts/token_tracker.py`、`token_budget.py`、`api_guard.py`、`spawn_guard.py` | 防止大任务失控 | 保留，纳入大任务前置检查 |
| 多 Agent 审计平台 | `audit-blackboard/` | 项目调度、疑点收集、报告生成 | 保留，适合后续项目化重构 |
| 场景技能地图 | `SCENARIO-SKILL-MAP.md` | 业务场景到技能/资料的路由 | 保留，与 RONGCE_AI_HUB 对齐 |
| 工作区记忆 | `MEMORY.md`、`memory/` | 用户偏好、业务范围、系统教训 | 保留，定期压缩维护 |

## 4. 半成品/需整理后使用的资产

| 资产组 | 主要路径/文件 | 当前问题 | 整理建议 |
|---|---|---|---|
| 百度 OCR / 云 OCR 流水线 | `scripts/baidu_ocr_client.py`、`baidu_doc_parse_client.py`、`ocr_cloud_pipeline.py`、`outputs/cloud_ocr_test/` | 近期活跃，但还像测试工程 | 建立 `scripts/ocr/README.md`，明确输入、输出、费用、适用场景 |
| 第6期审计案例 OCR | `outputs/audit_case_6*_ocr/`、`knowledge/审计案例-OCR/` | 已产出，但目录名乱码，需确认最终归档口径 | 保留文本和 Markdown，图片页可作为二级归档 |
| 内部文本分析工具一期 | 根目录 `内部文本分析审计工具一期功能清单.md` | 是产品规划资产，但在根目录散放 | 移入 `RONGCE_AI_HUB/products/text-audit-tool/` |
| 内部业务发展行动方案 | 根目录 `内部十项...业务发展行动方案.md` | 公司管理资产，未纳入中枢 | 移入 `RONGCE_AI_HUB/company-management/` |
| 三资/财政/交通场景脚本 | `scripts/build_scene_playbooks.py`、`build_priority_scene_*`、`enhance_sanzi_bid_v9.py` | 有业务价值，但脚本命名偏过程化 | 收敛成“场景产品包”目录 |
| 林芝投标分析脚本 | 根目录 `linzhi_*`、`outputs/linzhi_high_standard_farmland/` | 项目型成果，有复用价值 | 归入 `projects/linzhi_bid_analysis/` 或 `archive/projects/` |
| 医疗审计输出脚本 | `generate_medical_audit_outputs.py`、`generate_medical_audit_xlsxwriter.py` | 项目型脚本，可复用但缺说明 | 放入 `scripts/domain/medical_audit/` |
| 政策 OCR/条款补充 | 根目录 `extract_policies.py`、`update_clause_details.py`、`clauses_hits.txt`、`ocr_policy_text/` | 中间数据和脚本混放 | 成果入 `knowledge/policies/`，中间文件入归档 |
| Office/PPT/Excel 生成脚本 | `scripts/gen_ppt_*`、`generate_*xlsx*` 等 | 历史脚本多版本并存 | 保留最终版，旧版本入 `archive/scripts/office/` |

## 5. 建议归档的资产

| 资产 | 路径 | 归档理由 | 建议位置 |
|---|---|---|---|
| 历史项目大输出 | `output/contract_analysis/`、`output/bid_analysis_20260528/`、`output/bid-analysis/` | 体量大，属于历史项目或中间成果 | `archive/projects/` 或 `output_archive/` |
| 旧搜索索引 | `output/search_index/` | 可重建，占约 380MB | `archive/indexes/`，保留最新即可 |
| 微信文章抓取 HTML | 根目录 `tmp_wechat_article*.html`、`baidu_search_wechat_article.html`、`sogou_search_wechat_article.html` | 抓取中间文件，根目录干扰大 | `archive/web_fetch/2026-07/` |
| 磁盘清理记录 | `disk_analyze_c_top_20260701.json` 等 | 有历史参考价值，但非日常入口 | `logs/disk-cleanup/` |
| OCR 测试图片/页图 | `outputs/*/images/`、`page_images/` | 可追溯但不常用，占体量 | 成果保留，页图压缩归档或按需删除 |
| 多版本测试脚本 | `test_*.py`、`fix_*`、`tmp_*` | 调试遗留，影响脚本目录可读性 | `archive/scripts/debug/` |
| Python 缓存 | `__pycache__/`、`scripts/__pycache__/` | 可自动生成 | 可清理，无需归档 |

## 6. 建议删除/清理候选（需确认后执行）

| 类型 | 示例 | 理由 | 风险 |
|---|---|---|---|
| Python 缓存 | `__pycache__/`、`*.pyc` | 可再生成 | 低 |
| 临时抓取 HTML | `tmp_wechat_article*.html`、搜索页 HTML | 体积较大，通常只为一次提取服务 | 中，需确认文本成果已保留 |
| OCR 页图 | `outputs/*/page_images/` | Markdown/TXT 已生成时页图可归档或删 | 中，若需复核原图则要保留 |
| 旧 RAG/搜索索引 | `.rag_index/rag_index.json`、`output/search_index/*.pkl` | 可重建但耗时 | 中，删除前应确认重建脚本可用 |
| 重复脚本版本 | `gen_ppt_v5.py`、`gen_ppt_v5b.py`、`gen_ppt_v6.py` 等 | 多版本堆积 | 中，先标注最终版再归档旧版 |

## 7. 建议建立的新目录结构

建议先新增目录，不急着删旧文件：

```text
workspace/
├── RONGCE_AI_HUB/
│   ├── products/
│   ├── models/
│   ├── rule-library/
│   └── indexes/
├── projects/
│   ├── linzhi_bid_analysis/
│   ├── sanzi_bid_package/
│   └── medical_audit_outputs/
├── scripts/
│   ├── rag/
│   ├── ocr/
│   ├── case_collection/
│   ├── report_review/
│   ├── bidding_analysis/
│   ├── office_generation/
│   └── guards/
├── output/
├── outputs/
├── archive/
│   ├── projects/
│   ├── scripts/
│   ├── web_fetch/
│   └── indexes/
└── tmp/
```

## 8. 脚本分层建议

| 分组 | 应纳入脚本 | 说明 |
|---|---|---|
| RAG 知识库 | `rag_*`、`rag_config.py` | 重建、查询、服务启动 |
| OCR | `ocr_*`、`baidu_*ocr*`、`paddleocr_*`、`abbyy_*` | 本地/云 OCR、页图、文本导出 |
| 案例采集 | `case_collector.py`、`case_classifier.py`、`case_archiver.py` | 周期性采集和归档 |
| 报告复核 | `audit_report_review.py`、`gen_audit_review*.py` | 报告质控、复核表生成 |
| 招投标/串标 | `bid_*`、`linzhi_*`、`tfidf_*`、`metadata_*` | 文本雷同、图片哈希、元数据 |
| Office 生成 | `gen_ppt_*`、`generate_*xlsx*`、`*_docx*` | 成果模板、PPT、Word、Excel |
| 成本/Token 守卫 | `token_*`、`api_guard.py`、`spawn_guard.py`、`deepseek_*guard*` | 大任务前置控制 |
| 历史/调试 | `test_*`、`fix_*`、`tmp_*` | 归档，不作为主入口 |

## 9. 优先级行动清单

### P0：先做索引，不动文件

- [ ] 给 `scripts/` 生成脚本清单：文件名、用途、状态、入口命令。
- [ ] 给 `output/outputs` 生成成果清单：项目名、最终成果、中间件、是否可归档。
- [ ] 给 `knowledge/` 生成资料清单：资料类型、业务线、来源、是否已入 RAG。

### P1：收拾根目录

- [ ] 把根目录临时 HTML/TXT 移入 `archive/web_fetch/2026-07/`。
- [ ] 把项目型脚本和成果移入 `projects/`。
- [ ] 把管理类/产品类 Markdown 移入 `RONGCE_AI_HUB/products/` 或 `company-management/`。

### P2：脚本工程化

- [ ] `scripts/` 按 RAG/OCR/案例采集/报告复核/串标/Office/守卫分目录。
- [ ] 每个目录放一个 `README.md`，写清楚输入、输出、命令、注意事项。
- [ ] 旧版本脚本只保留最终版在主路径，其他放 `archive/scripts/`。

### P3：清理可重建文件

- [ ] 清理 `__pycache__` 和 `.pyc`。
- [ ] 对 RAG/搜索索引做“保留最新、旧版归档”。
- [ ] 对 OCR 页图做压缩归档策略：最终文本保留，页图按项目归档。

## 10. 直接可执行的下一步

建议下一步先生成三张机器可读台账，不移动文件：

1. `asset_inventory_scripts.csv`：脚本台账。
2. `asset_inventory_outputs.csv`：成果/输出台账。
3. `asset_inventory_root_cleanup.csv`：根目录清理候选台账。

生成台账后，再按“确认归档清单 -> 移动文件 -> 更新 README -> 清理缓存”的顺序执行。这样不会误删成果，也能把现有资产从杂物堆变成可运营资产库。
